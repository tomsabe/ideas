---
layout: page
title: Latest Research
description: Long-form investment analysis on public equities — generated by Tenzing.
permalink: /
---

<ul class="post-list">
  {% for post in site.posts %}
    <li class="post-card">
      <p class="meta-line">{{ post.date | date: "%Y-%m-%d" }}</p>
      <h3><a href="{{ post.url | relative_url }}">{{ post.title }}</a></h3>
      {% if post.summary %}
        <p>{{ post.summary }}</p>
      {% else %}
        <p>{{ post.excerpt | strip_html | strip_newlines | truncate: 180 }}</p>
      {% endif %}
    </li>
  {% endfor %}
</ul>
